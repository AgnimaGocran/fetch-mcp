{
  "name": "mcp-fetch-server",
  "version": "1.1.2",
  "description": "MCP server and CLI for fetching web content as HTML, Markdown, plain text, JSON, or YouTube transcripts",
  "type": "module",
  "main": "dist/index.js",
  "license": "MIT",
  "author": "zcaceres (@zachcaceres zach.dev)",
  "contributors": [
    "pwilkin (ilintar@gmail.com)"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/zcaceres/fetch-mcp.git"
  },
  "homepage": "https://github.com/zcaceres/fetch-mcp#readme",
  "bugs": {
    "url": "https://github.com/zcaceres/fetch-mcp/issues"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "bun build --compile --minify-whitespace --minify-syntax --target bun --outfile dist/fetch-server src/index.ts && bun build --compile --minify-whitespace --minify-syntax --target bun --outfile dist/fetch-cli src/cli/index.ts",
    "prepublishOnly": "bun run build",
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "publish:npm": "bun run build && bun test && npm publish"
  },
  "bin": {
    "mcp-fetch-server": "dist/fetch-server",
    "mcp-fetch": "dist/fetch-cli"
  },
  "engines": {
    "bun": ">=1.0"
  },
  "keywords": [
    "mcp",
    "fetch",
    "web",
    "scrape",
    "markdown",
    "youtube",
    "transcript"
  ],
  "dependencies": {
    "@cfworker/json-schema": "^2.0.0",
    "@modelcontextprotocol/server": "2.0.0-alpha.2",
    "@mozilla/readability": "^0.6.0",
    "@types/node": "^25.8.0",
    "jsdom": "^28.1.0",
    "private-ip": "^3.0.2",
    "turndown": "^7.2.2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/jsdom": "^28.0.0",
    "@types/mozilla__readability": "^0.4.2",
    "@types/turndown": "^5.0.6"
  }
}
